class Exam {
  constructor(token, date, patient, doctor, tests) {
    this.token = token;
    this.date = date;
    this.patient = patient;
    this.doctor = doctor;
    this.tests = tests;
  }
};

export default Exam;
